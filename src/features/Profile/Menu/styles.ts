import styled from 'styled-components'

import { Content } from '@core'

export const ProfileMenuCotainer = styled(Content)`
  background: ${({ theme }) => theme.colors.background_secondary};
  border-radius: 6px;
`
